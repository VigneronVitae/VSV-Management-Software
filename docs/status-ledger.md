---
Type: record
Purpose: "Records the single source of build truth for the winery app, so every other document cites status here rather than re-asserting it."
Depends on: [packages/cellar/docs/spec.md, docs/sorry-ledger.md, docs/compost-ledger.md, docs/methodology-lineage.md, supabase/migrations/0001_core_schema.sql, supabase/migrations/0002_derived_and_rls.sql, supabase/migrations/0003_parties_and_products.sql, supabase/migrations/0004_terms_and_effects.sql, supabase/migrations/0005_account_and_walk.sql, supabase/migrations/0006_vessel_thermal.sql, supabase/migrations/0007_vessel_edit.sql, supabase/migrations/0008_fill_vessel.sql, supabase/migrations/0009_vessel_type_form.sql, supabase/migrations/0010_glycol_by_type.sql, supabase/migrations/0011_vessel_type_fields.sql, supabase/migrations/0012_vessel_type_notes.sql, supabase/migrations/0013_close_on_empty.sql, supabase/migrations/0014_rack.sql, supabase/migrations/0015_fork_and_history.sql, supabase/migrations/0016_lot_owner_name.sql, supabase/migrations/0017_vessel_state_rls.sql, supabase/migrations/0018_lot_privacy.sql, supabase/migrations/0019_procedures.sql, supabase/migrations/0020_pin_search_path.sql, supabase/migrations/0021_cellar_write_paths.sql, docs/architecture-rulings.md, docs/findings-ledger.md, tests/schema_assertions.sql, supabase/migrations/0022_admission_and_authorship.sql, scripts/verify.sh, scripts/green.sh, tests/shim.sql, supabase/migrations/0023_subject_resolver.sql, supabase/migrations/0024_task_board_via_registry.sql, supabase/migrations/0025_bind_an_unbound_code.sql, supabase/migrations/0026_subject_type_registry.sql, supabase/migrations/0027_term_kind_registry.sql, scripts/db-backup.sh, scripts/db-restore.sh, docs/review/2026-09-14-export-red-team.md, docs/practice-mode.md, docs/review/2026-09-15-what-other-apps-do.md, docs/getting-somebody-else-on-it.md, scripts/watchdog.ps1, scripts/watchdog-loop.ps1, scripts/watchdog-install.ps1]
Depended on by: [CLAUDE.md, README.md, scripts/verify.sh]
---

# Status Ledger

## In one read

Some of this is built and most of it is not, and the grades below are the only place that says which. This ledger began by saying nothing was built, which was true when it was written and stopped being true without the sentence changing, which is the failure this file exists to prevent and committed itself. The point stands: the
boundary between designed and built should be a checkable fact from the first commit
rather than something reconstructed later.

The design is complete enough to build against and is not complete enough to be
correct. The sorry ledger holds the count and the detail; this ledger does not
repeat it. Two entries there, the topping lineage threshold and the
conditional-interval model, will change the schema when they are answered.

**The deadline is harvest**, which is weeks out and does not move. Build order in
spec.md section 7 is ordered by irrecoverability of failure, not by architectural
foundation: a bug in the board costs an afternoon, a bug in intake costs a bin that
cannot be reconstructed.

## Grades

**Built and verified.** Coded, and its check passes.
**In progress.** The boundary exists and the remainder is named.
**Specified.** Designed and named, ready to build.
**Deferred.** Named on purpose, with its condition for reactivation in the compost ledger.

A grade may carry a qualifier after a comma, as in "Built and verified, kernel only" or
"Deferred, partially reactivated". The grade is the part before the comma and is one of the
four above; the qualifier narrows the claim and never widens it. X-3-14 found seven distinct
strings in the grade column against four definitions, and this is the rule they were already
following without it being written down.

## Stage 0: schema and structure

| Component | Grade | Notes |
|---|---|---|
| Core schema migration | Built and verified | `0001_core_schema.sql`. This row said "written, never run" for nine sessions while `bun run green` applied it from empty on every one of them. W-10 added the check in `verify.sh` that catches a migration graded Specified, because a claim that was true when written and stopped being true without the sentence changing is the exact failure this file's own opening paragraph says it exists to prevent |
| Derived views and functions | Built and verified | `0002_derived_and_rls.sql`. As above. Its policies are the ones `0028` narrowed and the ones the blanket-policy disposition list judges |
| Row-level security | In progress | Policies exercised with three accounts in `tests/schema_assertions.sql`. The Supabase auth wiring above them is still unverified. See S-7 |
| Schema assertions | Built and verified | `tests/schema_assertions.sql`, all passing, against the cellar database and against an empty one. Until now the suite ran only from empty: three assertions asserted properties of a winery that does not exist yet, so the first facility party made the suite red and the definition of done contradicted itself, since it says run the assertions and says use `db:up` because reset takes the inventory. The suite now stands any existing facility party down for the run and rolls it back, and the first-claimant rule asserts both halves |
| Cellar write paths | Built and verified | `0021_cellar_write_paths.sql`. 0002 made every update on `node`, `placement` and `vessel` admin only, and every kernel function written since 0007 runs as the caller, so racking, forking and setting a jacket were zero-row no-ops for a cellar user with no error. `is_cellar_staff()` widens the rows and a column allow-list on three triggers keeps `owner_id` and `hidden` out of reach, refusing in words and naming the column. Predicted by A14 in the review corpus four days before the racking screen existed. See S-36 and S-37 |
| Admission and authorship | Built and verified | `0022_admission_and_authorship.sql`. The four cheapest exploitable findings in the review corpus, closed together. `is_facility_user()` coalesced a missing party row to true, so deactivating a client promoted that login to full cellar read: probed before and after, 0 nodes visible while active and the facility's lot visible once switched off. Asking "are you a client" instead of "is your party a facility" removes the default entirely and discharges S-25 as well, since a token with no `app_user` row is now nobody. `event_insert` accepted any row whose `by_sensor` was not null, so a string in that column let anyone write history in somebody else's name: probed, then refused. Both vessel-photos policies admitted every login eighteen lines under the comment explaining why the bucket is private. `claim_task` was security definer with no view on the caller, so a null identity wedged a task permanently and an assigned task could be taken by a passer-by. See S-39, S-40 |
| Subject resolvers | Built and verified, kernel only | `0023_subject_resolver.sql`. `AR-E5`. `task.subject_type` and `task.subject_id` are a polymorphic pointer with no foreign key, which is structurally forced: a core table cannot reference a module that may not be installed. A module now registers a resolver as a row, and `relation` is text rather than `regclass` on purpose, because a regclass column would put the dependency back into `pg_depend`. Resolution deflates at every step: no resolver, no relation, no row, or a row the caller cannot see all answer null and none raises. Probed by dropping `block` and watching resolution go quiet. No screen reads it yet; phase 4 is what makes `task_board` use it. See S-41, S-42 |
| `task_board` through the registry | Built and verified | `0024_task_board_via_registry.sql`. `AR-E6` named this view as the single declarative reason core cannot install alone: its `CASE` over `subject_type` named `node`, `vessel`, `location` and `block`, and Postgres recorded that in `pg_rewrite`. Proven twice. The board returns byte-identical rows before and after across all four subject types plus a task whose subject does not exist, and `drop table block cascade` no longer reports a cascade to it, where it did an hour earlier. Two assertions hold it: one drops a module and checks the view survives, one reads `pg_depend` so it fails on the change rather than on the consequence. See S-42 |
| A cellar hand may label a barrel | Built and verified | `0025_bind_an_unbound_code.sql`. Ledger A22, ruled by the winemaker: bind an unbound code as anyone who works here, rebind one only as an admin, and the refusal names the barrel that already has the sticker. Done the way `0021` did it, a narrow insert policy rather than a blanket definer, so update and delete stay admin and the table cannot be rebound directly either. Both halves asserted, including the wording of the refusal. See S-43, because the permitted path is silent |
| `subject_type` is a row, not a type | Built and verified | `0026_subject_type_registry.sql`. `AR-E7`, first half. The enum had four labels, three naming tables that belong to modules an install may not have, and it never failed to install, which is the combination `AR-E7` describes. The registry already existed: `0023` made `subject_resolver` keyed by subject type, so this deletes the enum rather than building a second registry beside it, and registering a resolver is now what brings a subject type into existence. Three foreign keys with `on delete restrict`, so uninstalling a module while tasks still point at its subjects is refused rather than cascading. Proven on a populated copy of the cellar before the cellar. Asserted: the enum is gone, an unregistered subject type is refused, registering one is a single row that the board renders immediately, and deregistering one with live tasks is refused |
| `term_kind` is a row, not a type | Built and verified | `0027_term_kind_registry.sql`. `AR-E7`, second half and much the larger: nine generated columns, nine composite foreign keys, four views, a view-returning function, three constraints, an index and two typed defaults. Enumerated from the catalog rather than listed by hand, following `0020`. Six of the eight kinds belong to winemaking or inventory, which the registry now records per kind. Two things this migration had to get right and nearly did not: the generated columns are altered in place rather than dropped and re-added, because `vessel_state` reads `visible_node` through a positional alias list and reordering `node` silently rebound `product_type_id` to `hidden`; and `validate_vessel_type_fields` proved a vocabulary exists by casting to the enum and catching the failure, which a blind rewrite to `::text` would have turned into a no-op. Both are asserted |
| The suite polices the schema | In progress | Eight mutation classes in `scripts/mutate.sh`, measured rather than estimated. Row level security could be disabled on 16 of 21 tables in September and cannot be disabled on any of 22 now. The classes are deliberately split between existence and behaviour: `policy` drops a policy and `weaken` leaves it in place with a predicate of true, `check` drops a constraint and `loosen` leaves it in place as `check (true)`, because a pinned inventory catches the first of each pair and only a probe catches the second. **Downgraded from Built and verified by W-6.** X-1 measured that 86 of the 198 catches were a pinned catalog comparison rather than anything exercising the schema, so the behavioural score is what the harness now reports and the gate. The counts that used to sit in this cell are gone rather than updated: a number in a sentence rots, and `bun run green` prints the current ones |
| Verifying the repository against itself | Built and verified | `scripts/verify.sh`, `bun run verify`. Seven checks: the typed header graph in both directions, the em dash rule, ledger cross references, counts stated in prose, that every command in the definition of done resolves, that this ledger does not contradict its own grades, and the module import rule. Each one was broken on purpose and observed to fail before being trusted. Its first run found forty nine one-directional header edges, which is the second time that invariant has been found broken. The module rule holds vacuously with one module besides core and says so rather than printing a green tick. No CI yet, by instruction: the checks had to exist before a pipeline could run them |
| Green as one command | Built and verified | `scripts/green.sh`, `bun run green`. Six things: verify, typecheck, lint, every migration from empty into a scratch database, the assertion suite against that database, and the assertion suite against a copy of the cellar. It reads the cellar with `pg_dump` and writes to it never. The two assertion counts differ by exactly one, the storage skip, and the script fails if they differ by anything else |
| The auth shim | Built and verified | `tests/shim.sql`. Three review runs reconstructed one, none committed it, and four grades rested on it for eight sessions, which is D1. Built to the W-1 specification, and its header says which migration needs which part: `0002` cannot apply without the realtime publication and has no guard, while `0005` and `0022` guard their storage blocks and skip with a notice. Discharges D1 |
| Account claim and walk kernel | Built and verified | `0005_account_and_walk.sql`. `claim_account` is asserted both ways, first claimant admin and every later one cellar with an invite, and W-8 walked the screens above it in a browser. The Postgres 16 in the old note was stale twice over: `config.toml` pins 17 and green runs there |
| Letting somebody in | Built and verified | `0068_an_invite_to_claim.sql` and `0069`. Claiming an account needs a six character invite an administrator hands out, so that reaching the sign-up page stops being the same thing as working here. Only an administrator issues one, and only an administrator can read the list, because a code a cellar hand can read is a code they can use to admit their own second account. Good for a week, usable once, and stamped with who used it rather than deleted: who admitted whom is the only record there will ever be. The first claim on a fresh install still needs none, because there is nobody to issue one. Eight assertions, and the old one-argument signature is dropped rather than left, because a second signature here would be a call that skips a credential check. Discharges S-78 and does nothing for S-77, which is the one that gates going public |
| Colour, and what it makes of a barrel | Built and verified | `0071` through `0076`. A wine says whether it is red, orange, rose or white, and a barrel's own colour is derived from what it has held since it was last reconditioned. Never stored: sixteen call sites put wine in a vessel and none of them was touched. Three states rather than two, because a barrel that has held a lot nobody typed is `unknown` and calling that white is the A25 class. Nothing refuses a fill, on the winemaker's instruction: a barrel can be filled before anybody records it, so the catch is a worklist. Which colours stain is an attribute on the vocabulary, so a fifth colour is an insert. Sixteen assertions, three of which found real defects in the first cut: a null-permit in `colour_stains`, an arbitrary winner between two disagreeing parents, and a boundary tie that called a red barrel white. See S-80 |
| The wine in a vessel | Built and verified | `0078`. "I also need a way to edit (add/append only is fine) wine in vessels, like to add the color." There was nowhere to do that: tapping a full vessel went to the vessel edit screen, under a comment saying that was the only thing left to do to it, which stopped being true the moment a lot had facts somebody says after intake. Both full and empty vessels now offer a choice, and the wine is the first thing on it. `lot_detail` is one read rather than four, with the colour resolved through the lineage and a flag saying whether anybody said it about this lot. Append only: a colour, a vintage, and notes, which are the untyped floor from `0064` |
| Thermal state on the map | Built and verified, one half shipped dead and one rule replaced | `0079`. Vessels held cold draw light with a snowflake and their setpoint, held warm the inverse, and a controlled room tints the negative space its vessels stand on. The vessel half needed no schema: `vessel.mode` has been cooling, heating or off since `0006` and this client's type never declared it, so nothing could draw it. The room half could not be expressed at all, and no temperature settles it: 15.5C is cooling in September and heating in January, so a room says which way it is held, out of the vessel's own vocabulary. Nothing is backfilled, and a controlled room with no direction draws its temperature and no colour. **The room half shipped doing nothing and was found two phases later.** `cellarMapLayout` defaults `climate` to an empty array, the call site never passed it, and a default is why that typechecked and why no room has ever been tinted. Fixed while building the batch selection. The vessel half worked throughout. **And the rule was wrong.** `0079` asked somebody to say which way a room was held, reasoning that 15.5C is cooling in September and heating in January. That is true of the outside, and the winemaker corrected it: "a room is departure from room temperature not outside temperature". `0097` derives it from how far the room sits from what a room is here, measured from the uncontrolled rooms or 20C, with two degrees of slack so a room at room temperature is not called cold. The told direction survives as an override and the row says which it is. The floor wash went with it: "I actually don't like the current tint, it makes it hard to see", so a room is marked at its edge where nothing is drawn on top of it. Thermal is an outline, barrel colour an inset rim and volume a fill height, three channels that do not collide |
| Correcting whose wine a lot is | Built and verified | `0080`. Eighteen functions read `owner_id` and every one that writes it writes it at creation, so a pick filed under the wrong party could only be fixed by an update against the table. It is not cosmetic: `owner_id` is what `visible_node` reads, so a wrong one is either a client who cannot see their own fruit or one who can see somebody else's. The correction writes an event saying who made it and why. Descendants are counted, never cascaded: a child belonging to another party is what a custom crush arrangement looks like. See S-81 |
| A borrowed bin is not ours | Built and verified | `0081`. The winemaker registered five bins on loan from a grower and every screen said they were his. What he typed was recorded correctly; `facility_owned` was `owner_id is null`, and a grower is not a party, so the unticked box and the ticked box produced the same visible answer. A13 in its purest form. Ownership has two representations, a party for a client's equipment and free text for a grower's, and only one was being read. Display only: no policy reads `facility_owned`, and the column order is unchanged because the view binds `visible_node` positionally. See S-82 |
| Three kinds of sampling | Built and verified | `0082` through `0084`. Vineyard, juice and wine, derived from what was sampled rather than chosen when it is taken: a vineyard, block or planting is fruit on the vine, and a vessel is juice or wine depending on the lot that was in it. The lot is the one in the vessel at the time, so refilling a barrel does not rewrite what last spring's sample was of. `sample_target` gives the picker the same derivation, so a filter that hides last vintage's barrels cannot keep offering them; it also replaced four client round trips and a set of group headings that lived only in this client. Filters persist per device and an empty selection means all three. See S-83 |
| Picking bins as inventory | Built and verified | `0085` and `0086`. "I'd rather just inventory and add as they don't really differ." The batch existed and only ever created bins onto a pick, so the fleet got registered in dribs during harvest. `register_bins` takes a count and a prefix and nothing else: the type, the capacity and the numbering come from the stack already there, and numbering carries on from the highest worn so a retired bin's name is never reused. `bin_inventory` counts rather than lists, split by whose they are. `0086` fixes a local variable named `type_id` colliding with the column, which the assertion found on its first call: the same class as the `where code = code` two sessions ago |
| Adding a vessel takes a count | Built and verified | `0098`. "Seems like all it needs to be is a number of vessel in the type. Then it defaults to 1 but you can change it to whatever you need for an action." `register_bins` was the only thing in the app that took a count, so twelve barrels off a pallet were twelve trips through a form whose answers were the same every time. `add_vessels` takes a payload and a number, one unless somebody says otherwise, and the numbering rule now lives in exactly one place: `register_bins` calls it rather than repeating it, and the client's direct table insert is gone, which is one fewer write path outside the kernel. A name already wearing a number is read as the series it belongs to, so typing PB1 with a count of five continues PB rather than starting a PB1 series beside it. A code and a photograph belong to one object, so the screen takes both away as soon as the count passes one. **And the naming convention.** "Picking bins on loan should have a different naming convention, eventually I'm gonna have like 30 picking bins of ours in there." A borrowed vessel nobody names is called after whoever lent it, first word cut to four letters, so Pearlstaad's next stack is PEAR1 onward and his own thirty keep PB. Nothing is renamed: PB4 to PB8 are in a press log and on the pad under those numbers today. See S-87 |
| A front door, and the cellar moves to /cellar/ | Built and verified | `0109` to `0111`. "Could we have /cellar/ be the cellar one and the root be the chooser that you pick cellar/shop/inventory/marketing/get new modules?" The module column has been on every readable and capability since `0027` and nothing had ever read it; `module` says what each one is and where its periphery lives, and a null path is a module the kernel knows with nothing built on it, which is the honest version of "get new modules". How much is in each is counted from the contract, never declared. **Three defects found by looking at the page rather than by reasoning.** The list was scoped to `authenticated` and the front door draws before sign-in, so a first-time visitor got a heading and nothing to pick: `0110`. Then the counts read zero for the same reason one layer down, printing "0 things you can do" beside a module with thirty one, which is A25 in its counting form: an empty set caused by permission is indistinguishable from an empty set that is true, and `0111` counts as the owner. Then a screenshot showed every list row rendering as run-together inline text, because `.row`, `.row-main`, `.row-note`, `.row-title` and `.link` had been emitted across several screens all session and existed in no stylesheet, the glycol screen included. Text extraction had passed every time: it proves the data arrived and says nothing about the page. The stylesheet is now one file in core that three apps import, after being copied twice. The cellar's service worker moved with it, and the front door unregisters the old root-scoped one so nobody opens the chooser and gets a cached cellar. See S-94 |
| The shop, as a second periphery | Built, unrun on a phone | "I think the shop periphery, because I think two peripheries will run into problems and find good solutions better than one." `packages/shop` and `apps/shop`: its own manifest, its own icon, its own colour, served under `/shop/` so it shares an origin with the cellar app and one sign-in serves both, because localStorage is per origin and an intern who has to sign in twice signs in never. **It made an existing check start working.** `verify`'s module import rule had passed vacuously since it was written, there being one module and no sibling to violate it with; it now reports two ordered pairs and means something. The DOM primitives moved from `cellar/ui.ts` down to `core/ui.ts`, which is CLAUDE.md's rule about shared code and the first time there was a second module to apply it to; cellar keeps `screen()` because its chrome carries the practice band and the shop's does not. `0107` registers the shop's five screens so a note can point at one, after the winemaker said Matt would be the biggest user and the feedback path had been left out. Two gaps filed from building it: S-92, a model with no machine could not be chosen, wrong on the first run and discharged the same day by `0108`; and S-93, the shop has no router, which is the tab-discard failure the cellar already learned and has not yet been given the same answer |
| A machine is a departure from its model | Kernel built and verified, no screen | `0105` and `0106`. "There should be an app that isn't maintenance exactly, but lineage of equipment including maintenance", for "machinery, or things that need fixed and diagnosed. Tractor and tractor equipment, press, sorting line". The design is two phrases of his. **Unique and not:** a model holds what one of these is as shipped, a machine holds this particular one, the same bridge `vessel` and `vessel_type` already are. **Departure from something:** what a machine is now is its model with every modification applied over the top, derived by `machine_spec` and never stored, so the press reads single phase until the VFD install is recorded and three phase after, without anybody editing a field. A second bridge keeps the press one object: `machine.vessel_id` claims the vessel `0053` already made, and a vessel cannot be claimed twice. Work is dated when it happened rather than when it was typed, because the VFDs went in months before this existed. A repair carrying a spec change is refused rather than ignored, which would be a write that reads as saved and does nothing. `quirk` is a kind of entry beside repair and service, because the old panel with the numbered programs is not maintenance and is what somebody needs at six in the morning. First module that is not about wine, `shop`; ten assertions. **No screen yet**, and the winemaker's own framing is that each module is its own periphery on his phone: same kernel, same origin so one sign-in, its own manifest and icon. See S-91: lineage, not a maintenance planner |
| The cellar restarts itself | Built and verified | The cellar was offline for eight hours overnight and the winemaker found it, not the app. Two things lined up: the Claude desktop app updated itself at 23:42 and its VM service was reinstalled at 23:53:11, which took the shared WSL platform down and Docker Desktop logged `enginedependencies: starting graceful shutdown` at that exact second; then Docker could not come back, because an agent had written `settings-store.json` with a UTF-8 byte order mark while enabling autostart and Docker's parser refuses one. That file is only read at startup, so the damage lay dormant for hours. Autostart was never going to help either way: it fires at login and the machine had been up three days. `scripts/watchdog.ps1` asks whether the database answers and starts what is missing when it does not, tested on the healthy path, a forced recovery and a genuine unrecoverable failure. **It is not a scheduled task, and not by choice:** Task Scheduler on this machine accepts tasks, reports running them, and never starts the process, including one whose whole action is `cmd.exe /c echo` into a file, which needs administrator rights to diagnose. So the interval lives in a loop started from the HKCU Run key, which is how Docker Desktop itself starts here. Five minutes, in one variable in `scripts/watchdog-install.ps1`. The tradeoff is stated rather than hidden: a loop in a process dies with the process and nothing restarts it until the next login |
| A pressed bin leaves the room | Built and verified | `0104`. Three things in one message. "I added 4 grundies for Domain Publique but they're showing up in bins to return": `0085` marked a bin borrowed when another party owned it, which is right for a bin because somebody else's bin goes back, and `0098` carried that line into `add_vessels` so every client-owned vessel became a thing to return. Four Domain Publique tanks were the first through the new path. Naming a lender still marks anything as going back; party ownership now does so only for bins. Underneath it, `bin_to_return` selected any empty borrowed vessel with nothing about it being a bin, so one tank marked on loan would have appeared on a screen called Bins to return whatever `add_vessels` did; it is typed now. "When I pressed the Pearlstad bins it should have taken the picking bins out of the cold room and emptied them": the emptying was `0090` and worked, the room did not, so five empty bins stayed drawn in a cold room they had been wheeled out of. They now go where the press stands, through `move_vessels` so a bin moved by a press and one moved by hand are moved by the same code, and nothing moves when the press has no room of its own. Five assertions. The four tanks, the five bins and the Pearlstaad spelling were all repaired by hand |
| Racking keeps what was already there | Built and verified | `0103`. "I added 50 L to a barrel from the press, then 150 L from a tank I pressed into (via racking). Now it's just showing the 150 L in that barrel." He was right and the wine never left the barrel. `rack` absorbed the resident lot into the blend as a parent and then gave the blend a placement of what arrived down the hose. `rack_plan` had counted the 50 L all along, which is why the lineage read 0.75 and 0.25 of 200 L while the volume read 150: every share was right and only the two numbers saying how much wine there is were wrong, by exactly what was already in the vessel. The placement now gains what that vessel held and the blend's quantity gains the total, because a lot whose quantity disagrees with its placements is what T0-2 is for. `in_l`, `out_l` and `loss_l` are untouched and still describe the transfer. Not retroactive: earlier racks into occupied vessels are still short, and a low placement is indistinguishable from a measured one, so no migration can find them. VS2610 was corrected by hand with an event, and a second event after it carrying `inferred`, because the first was written `observed` and nobody dipped the barrel. **Nearly reverted `0049`:** rack was rebuilt from `0014`, and `0049` had redefined the whole function in upper case where a lower case search does not find it. The assertion suite refused the next blend and named the constraint. Also client only: vessels sort by whose wine and by whose vessel, which are different questions at a custom crush winery, with a filter on whose wine |
| A tank takes more than one pressing | Built and verified | `0102`. Mid press: "it's not letting me press a second lot into the second tank as the first because 'Skinny Boy already holds wine'." The rule was already written down in the other place it applies: `0014` says a destination holding a different lot is a blend and not an error, and racking has worked that way since the beginning. `draw_cut` never learned it and refused, which is a refusal arriving while juice is running. Wider than the reported case: free run and hard press into one tank hit it too, because each cut is its own lot. The tank keeps its lot rather than minting a blend node, which is his answer and the one departure from rack: "it should carry the lineage from the pressings but also be just Pearlstaad chardonnay". Filling a tank over a day of pressing is one wine being made, and minting per draw would give four generations of lot for one tank. Shares are rescaled by contributed volume, the same arithmetic `0014` uses, so composition stays derived. A different variety is said on the result rather than refused, his barrels ruling again. Also client-only, from the same evening: racking records the gas in the destination as well as the source and the line, which is the half of an oxygen pickup record that was missing. See S-90: a corrected draw does not shrink a tank it was blended into |
| A note about anything, from any screen | Built and verified | `0101`. "I want to be able to add a note to anything. Maybe a permanent top right block to select something on any screen to take a note of? One of my first uses will be to make notes of verbage changes, so notes point to actual objects." Most of it existed: `add_note` has taken any registered subject since `0063` and refuses a type nothing is, so this is a registry change and a control, not a notes change. The hard half is the use he named. A note about wording is about a screen, and a screen was a string in a TypeScript file, so screens became rows, forty one of them. `term` and `glycol_machine` were registered at the same time: a term is where a variety name is actually written, which is where a note about calling it something else belongs. The button lives in `screen()`, so it exists once rather than being remembered on forty one screens, and the sheet defaults to the object the screen is about and falls back to the screen itself. `scripts/screens.sh` is green step 7 and is what makes the registry safe: a screen added without a row fails the gate rather than failing as a person tapping the button and being told nothing in this system is that. Shown to fail on that defect before being trusted. See S-89: a wording note points at the screen, not at the words |
| Glycol machines and what is on them | Built and verified | `0100`. "The glycol jackets should be linked to a glycol pump and chiller/cooler", and the winery: two machines, pump and cooler in one unit each, the bigger one able to heat but only one direction at a time. Seven vessels carry a jacket and three were cooling at 1C with nothing anywhere saying which machine was doing the work. A machine is one object, because that is how his are; a hookup is a period with a start and an open end, the shape `placement` has, because hoses move and he wanted the history. Which way a machine is running is derived from the jackets on it, the same move `0097` made for a room. A vessel held warm cannot go on a machine with no heater, refused at the hookup and again by a trigger at the moment somebody sets the mode, so the rule does not depend on doing the two in one order. A machine asked to cool and heat at once is let through and listed in `glycol_conflict` naming both sides, which is the barrel-colour ruling applied again: somebody can move a hose before telling the app. Sixteen assertions, one of which found that unhooking used the check constraint as its error message. See S-88: nothing in this app has ever touched a machine, so a unit switched off at the wall still reads as cooling |
| Vessels sorted and filtered by vintage | Built and verified | `0099`. "Another thing to add for sorting vessels, by vintage. Maybe just 2024/2025/2026/NV?" The years were already there: `vessel_state.vintage` has carried the year of the lot in the vessel since the view existed. NV was not. `0049` made a lot say a year or say it is deliberately non-vintage, and that distinction never reached this view, so NV, an empty vessel and a lot nobody has answered for arrived as one null. `non_vintage` and `vintage_label` are appended, the label chosen by the kernel so two screens cannot choose differently. The years in the filter are the ones the vessels are actually holding, newest first with NV after them, because a list of years in the client is a list somebody edits every September. A stale sticky choice clears itself rather than hiding every vessel behind a control reading blank. The column alias list on the lateral went too: `visible_node` returns a node row, and the positional list in `0091` had already fallen three columns behind `0071` |
| A bin holds pounds | Built and verified | `0087`. "Picking bins should hold fruit in lbs or % ton", and then the better version: "the bins should each have a fruit amount in lbs so we just use that right?" Pounds became the told fact and how full the derived one, which is the reverse of `0033`. Whichever half somebody says is stored and the other is worked out at read, enforced by a check so it does not depend on going through one function. A full bin is 850 lb, his "800 to 900 if it's bulging" with the middle taken, on the type so it moves without a release. Nothing is backfilled: existing bins keep the percentages somebody typed by eye and read back as estimates. The entry box toggles lb and percent, sticky per device. See S-84 |
| The map names the wine | Built and verified, unasserted | "Instead of saying the vessel name on the vessel it should say the wine in the vessel." A shape with room carries the lot name with the vessel small beneath it; a 52 pixel barrel carries the variety and year, which is what somebody says out loud in a barrel room, and names the vessel on tap. An empty vessel still shows its own name, which is when you want it. Checked by eye against the real stylesheet: the first cut overflowed the circle and clipped mid-word |
| Moving more than one | Built and verified | `0088`. "I'd love to be able to select 6 barrels to move to a new room, or put the 5 picking bins in the south bay, without doing it individually." The kernel had done this since `0044` under a name that said bins, with two assertions over it, no screen above it and an exemption reading not yet declared: asserted and unreachable. Renamed to `move_vessels`, declared, and given a selection mode on the vessel list. All or none: a batch naming a vessel that is not there moves none of them, because six selected and five moved is a state nobody would notice. Seven assertions |
| A press takes bins | Built and verified | `0090`. "I can't fit all 5 bins into one press." `0052` took whole lots and `0054` closed them, which was right while a pick was assumed to fit in a press. The source is now the vessels somebody pointed at and the lots are read off them, so a pick stays open with the bins that did not go in and is spent only when its last bin empties. The scale still wins: a weighed pick contributes its own figure apportioned by the share of its bins going in, not the sum of the estimates. The parameter was renamed rather than reused, so every caller had to be revisited. Nine assertions. See S-85 |
| What is in each bin, where you look | Built and verified, unasserted in the client | `0089` and `0091`. The per-bin weights existed after `0087` and no screen showed them: the pick screen listed which bins were unweighed and nothing else, and the vessel list measured every bin in litres, so a bin of fruit read as holding nothing. The pick screen now lists each bin with its pounds and tons and a box to correct one, and `vessel_state` carries the pounds, the tons and how full, so the list and the map say what is in a bin and draw its fill. The fill fraction is a kernel answer: the client dividing by 850 would be a screen holding a rule that lives on the vessel type |
| Gross or net, said out loud | Built and verified | `0092`. "It should be very clear whether the measurement is the net weight or fruit weight." `0087` asked for "fruit in each bin" and somebody at a pallet scale reads 923, which is the bin as well as the fruit: a 92 lb bin makes that 831, and both numbers are plausible weights for a bin of Chardonnay. The column is renamed to `net_lbs` to match the words `weigh_bins` has used since `0042`, a `gross_lbs` joins it, and exactly one of net, gross and percent may be set. The entry box no longer pre-fills 850, which is why five bins read 850 without anybody typing a weight. A bin can be filled past its nominal, because bins bulge. Ten assertions |
| A guess is not a weight | Built and verified | `0093`. He had already weighed every bin, one at a time, with a photograph of each, and `bin_fruit` reported 850s because `0087` multiplied a guessed percentage by a nominal and put the result in the same column as a figure off a scale. A bin now reports a weighing of that bin alone, then pounds somebody typed, and **never a percentage**: a bin nobody weighed has no weight and says so. The guess is kept beside what turned up, which is the only job it has left. A weighing covering several bins gives none of them a weight of its own, because one number split evenly is measurements nobody made. Six assertions |
| Bringing records in | Kernel built and verified, no screen | `0094`. "It needs to be able to ingest and format (even if tagged as LLM assisted) structured data as excel sheets or hand written or whatever", for his own back history and for the next winery's. **An import is a sequence of capability calls, not a pile of rows**: rows would walk past every refusal the kernel exists to make, calls go through the same guards as a person tapping a screen, and `contract()` already enumerates all 27 with their parameters, so an assistant can be written against it without anybody describing this schema. A batch names the document and what read it, so an assisted row stays marked as one; a step is checked against the contract when proposed rather than halfway through applying; a failure keeps the refusal, which is the kernel saying the document describes something that could not have happened. Nine assertions. No screen yet, and S-86: an applied batch cannot be undone as a batch, which is why every import is rehearsed in practice first |
| Every rpc argument checked against the kernel | Built and verified | `scripts/rpc-args.sh`, step 6 of the gate. A press failed in the winemaker's hand with "could not find the function public.start_press(p_detail, p_node, p_press_vessel_id, p_source_ids)": `0090` renamed that parameter on purpose so callers would break, the three in SQL did, and the one in TypeScript did not, because an rpc argument is a key in a bag nothing typechecks. 134 arguments checked against `pg_proc`. Shown to fail on the real defect via `VSV_RPC_SRC` rather than trusted to |
| What is running | Built and verified | `0095`. "A tab called running operations that has open things: press going, pick going, etc." Each of these was already a badge added one at a time, so knowing what was in flight meant knowing which badges to read. **Running is derived from the absence of an ending in every case** and is never a flag, so nothing can be left set by somebody who walked away. Procedure runs and tasks are branches with no rows today rather than an edit waiting to be forgotten |
| The hot list | Built and verified | `0096`. "Also similarly something like a hot list", and the example that defined it: "the first press just started. I imagine by the time it's finished it'll be more like 450 liters." A number he already had and the app had nowhere to put. Anything with a registered subject type can be watched, with or without an expectation; the expectation is told, sits beside what actually happened, and never becomes it. Changing your mind is a second statement, so how the day went survives. Nine assertions |
| Terms and operation effects | Built and verified | `0004_terms_and_effects.sql`. The vocabulary every picker reads, and the one `0027` converted from enums to registry rows. Asserted, and A24 is the open gap in it rather than in whether it runs |
| Vessel thermal state | Built and verified | `0006_vessel_thermal.sql`. The jacket columns have existed since 0001 and `vessel_state` has always read them; no write path set them, so every vessel was unjacketed and `effective_temp_c` always fell through to the room. Both vessel screens now offer a jacket, mode and setpoint, and two check constraints refuse a mode without either. See S-18 |
| Vessel editing | Built and verified | `0007_vessel_edit.sql`. `update_vessel` applies a patch and, when the thermal triple changes, writes a `setpoint_change` against the vessel stamped `observed`. Tapping a vessel in the list opens the create form with its current values in it. Renaming records nothing; turning a jacket on records once, and saving the same state again records nothing. See S-18 |
| Filling an existing vessel | Built and verified | `0008_fill_vessel.sql`. Placement was written in one place only, inside `create_vessel_with_wine`, so an empty vessel stayed empty for good. `fill_vessel` completes the other half of build order 1. Refuses an occupied vessel in words rather than as a unique index violation. Inventory only, no source: see S-19 |
| Vessel type form shape | Built and verified | `0009_vessel_type_form.sql`. "Creator of" is one function with two contracts: a barrel wants coopers and calls it Cooper, a tank wants manufacturers and calls it Manufacturer, and `makers_for_vessel_type` returns the intersection. Each type also declares which optional fields open by default; the rest sit one tap into "More details". A maker with no contract satisfies every contract, so nothing added before this disappears. A vessel type added at runtime gets the neutral default and cannot configure itself until the term screens exist. `0010_glycol_by_type.sql` puts the jacket under the same mechanism: a tank opens it, a barrel keeps it behind More details, and neither hides it, because visibility is not permission |
| Vessel type field editor | Built and verified | `0011_vessel_type_fields.sql` and `0012_vessel_type_notes.sql`. A vessel type carries an ordered list of field descriptors, so adding oxygen ingress to tanks is a row edit rather than a release. `validate_vessel_attributes` enforces required, min and max, and that a term value comes from that field's own contract; a second trigger refuses a descriptor that would break the form. Editing is admin only; anyone signed in may leave a note against a type, which an admin resolves. Units are display only, see S-20 |
| Racking | Built and verified | `0014_rack.sql`. One operation, because the cellar has one: the caller says which vessels wine came out of and which it went into, and the kernel reads the shape to decide whether a lot moved or a new one was made. `rack_plan` runs the same rule without writing, so a screen can say what is about to happen. Loss is derived and never stored. Capacity is the one refusal, overridable and recorded. The screen previews live as you add legs and repeats the kernel's answer rather than working it out. Two bugs were found by reading the database after a browser run and not by the assertions: a lot in the destination was closed even when it still lived in another vessel, and shares were divided by what arrived rather than by what the parents contributed, so loss changed composition. Both fixed, both now asserted |
| A lot closes when empty | Built and verified | `0013_close_on_empty.sql`. Replaces `lineage_closes_parent`, which closed a lot the first time it fed anything. Taking 228 L off a 2000 L lot now leaves 1772 L, open. Discharges S-3, and corrects spec.md, which carried the old rule |
| Forking a lot, and inherited history | Built and verified, kernel only | `0015_fork_and_history.sql`. Recording against some of a lot's vessels and not all of them forks those vessels into their own lot, because otherwise the record claims the extra sulphur reached all four barrels. Nobody picks a mode; the kernel reads it off the shape. `node_history` walks lineage with a cutoff at each edge, so a shard inherits what happened before it came off and nothing after. Nothing is copied. No screen yet beyond the result page |
| Skins | Built and verified | `packages/cellar/src/skins.ts` plus a scoped override layer in `app.css`. The word is the repo's own: apps/web/src/index.ts always said a later session would host several. A skin is a stylesheet selected by `data-skin` on the root, chosen from a picker on the home screen and remembered. Original is the foundation, untouched, so adding a skin cannot alter the one already in somebody's muscle memory. Cellar is the second: cards, 54px targets against Original's 48, and a fill gauge drawn from a `--fill` custom property the vessel list publishes whether or not a skin uses it. Neither overflows horizontally at 375px. A skin may change how a thing looks and never what is asked or in what order, because that order is the schema's |
| The menu you start from | Built and verified | Home is now a menu grouped into what the cellar does, what is set up, and what is not built yet. The unbuilt entries are listed rather than hidden, in spec.md section 7's order, because a named gap is worth more than a blank space and that order is by irrecoverability rather than by appetite. They are readable and not tappable. The vessel list moved to its own screen behind a Vessels entry that carries the count |
| Ownership of wine | Built and verified | `node.owner_id` and the `node_read` scoping existed from 0003 and nothing ever set them, so every lot was the facility's whatever the truth was. A Clients screen creates custom crush parties and attaches a login, and the wine form asks whose wine it is with add-inline for a client. `0016_lot_owner_name.sql` puts the owner's name on `vessel_state`, because the screens were showing the vessel's owner under the word Owner, which read as Facility on a barrel of somebody else's wine. Whose barrel and whose wine are independent in both directions, asserted across all four combinations, and what a client sees is decided by the wine and never by whose barrel it sits in. See S-25 |
| Views obey their policies | Built and verified | `0017_vessel_state_rls.sql`. `vessel_state` ran with its owner's rights, so RLS on the tables under it did nothing and both custom crush clients could read every lot in the cellar by name from the vessel list. One line to fix. The assertions all queried tables while the screens query views, which is why it survived eight sessions; there is now a check that every view in public is security_invoker. See S-26 |
| Lot privacy, per field | Built and verified, kernel only | `0018_lot_privacy.sql`. The owner of a lot names which of seven fields the crew may not see: name, owner, variety, vintage, attributes, history, composition. What is missing from that list is the floor: that a vessel holds wine and how much, because the work stops otherwise. Seeing and controlling are split, so an admin reads every lot and cannot change what a client hid. A party carries a default that new lots inherit. Redaction is done by `visible_node`, so it holds against the API and not only against a screen. No screen yet. See S-27 |
| Procedures with timed steps | Kernel only, no screen | `0019_procedures.sql`. Barrel steaming is the worked case: initial rinse, steam, bung suction, final rinse as timed steps with targets an admin sets, then a solution step drawing on the `material_kind` vocabulary. A session lays out one run per barrel in the order they are done. Duration is the difference between two timestamps and is never stored. Leaks, rings and off smells are events on the vessel, so they outlive the session that noticed them, and `vessel_history` reads them back. See S-28 |
| Not losing your data | Built and verified | `bun run db:up` applies pending migrations and leaves the cellar alone; `db:reset` destroys it and is for proving migrations from empty, not for a development loop. CLAUDE.md said the wrong one and now says so. `db:backup` and `db:restore` exist and were tested by wiping and restoring, and the restore is honestly imperfect: see S-29. `0020_pin_search_path.sql` fixes what that testing uncovered, which is that thirty four functions broke under the empty search_path every dump sets |
| Term configuration screens | Deferred, partially reactivated | Deliberate. Seed data is the editor and add-inline covers the case that blocks a fresh install. See compost ledger C-6 |
| Parties, ownership, and vessel codes | Built and verified | `0003_parties_and_products.sql`. It has run against this project's own instance since the wire session, and `node_read`, which it created, is the one policy that was scoped correctly while everything pointing at `node` was not. That is A5, partly closed by `0028` |
| Variety templates (seed) | Specified | Six protocols transcribed, not encoded |
| Location and vessel inventory | Specified | Requires a physical walk of the winery |
| Party records | Specified | The facility party and both custom crush clients. First thing the walk creates: `node.owner_id` defaults to the facility party, so no node can be inserted before it exists |

## Stage 1: capture

| Component | Grade | Notes |
|---|---|---|
| Backfill path, kernel | Built and verified | `create_vessel_with_wine`: vessel, lot, placement and codes in one transaction. Asserted |
| Backfill path, screens | Built and verified, unasserted | W-8 ran it. A fresh clone, an isolated stack, account to facility to vessel with wine to rack, and the database matched the screen at every step. Nothing replays it, so it is a row in `docs/status-claims.tsv` rather than a check, which `bun run green` now counts and prints |
| Sign up and account claim | In progress | `claim_account` asserted; the screens above it are unrun |
| First run, facility party | In progress | Screen written. It explains the wall rather than only showing a form |
| Code binding by camera | In progress | zxing for 1D and QR, with manual entry beside it rather than behind it. Never run against a camera |
| Inferred history generation | Built and verified | `generate_inferred_history` asserted. Generates nothing until a template exists, see S-17 |
| Intake screen | Specified | The one that must be solid before harvest |
| Press and destem | Specified | |
| Event entry | Specified | Manual transcription from Wine Meister |

## Stage 2: coordination

| Component | Grade | Notes |
|---|---|---|
| Tasks and board | Specified | Atomic claim function written in `0002` |
| Local-first sync | Specified | Append-only local, mutable server-side |
| QR codes on vessels | Specified | Barrels first; tanks are recognisable by name |
| Topping mode | Specified | Depends on QR |
| Cap management mode | Specified | Depends on QR and `cap_rule` |

## Stage 3: reconciliation

| Component | Grade | Notes |
|---|---|---|
| `doctor` | Specified | The only check on orphaned `subject_id` |
| Provenance audit view | Specified | What fraction of a number was never witnessed |
| Nightly git export | Specified | Backup and diffable daily record |
| Replay fixtures | Specified | Mid-harvest seed state for dev and forks |

## Stage 4: compliance and case goods

Nothing here is graded, because a grade would imply a design exists. Volume
losses (S-8), unit conversion (S-9), tax class (S-10), bond status (S-11), and
the bottling seam (S-12) are named in the sorry ledger and are blocked on the
winery's compliance advisor. They are listed as a stage so that their absence is
visible here rather than only in the sorry ledger.

The one with a date on it is S-12. Bottling happens whether or not the event that
records it is complete.

Activity costing (S-14) sits on top of all five and is not reachable until they are
answered. A cost per case computed from unreconciled volumes looks authoritative and
is not, which is the failure mode this whole stage is arranged to avoid.

## Specified, not scheduled

Section 8 of the spec, scouted from commercial platforms. Designed and named, and
absent from the section 7 build order on purpose: that order is by irrecoverability
of failure and none of these are irrecoverable.

| Component | Grade | Notes |
|---|---|---|
| Dry goods with live depletion | Specified | `material` and `material_use`. Not a purchase order system |
| Work orders | Specified | Grouping table plus a print view. Tasks stay standalone |
| Reverse block view | Specified | Derived, per T0-2. Same traversal as `block_composition` |
| Clone and rootstock on `block` | Specified | Structured fields, not `notes` |

## Deferred on purpose

Direct BLE to the EasyDens meter. See compost ledger C-1.
